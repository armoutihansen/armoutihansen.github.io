---
interface Props {
  publication: {
    title: string;
    authors: string;
    venue: string;
    year: string;
    details: string;
    href: string;
    type: string;
    abstract: string;
  };
}

const { publication } = Astro.props;
---

<article class="bib-item">
  <p class="meta-period">{publication.year} · {publication.type}</p>
  <h3>
    {publication.href
      ? <a href={publication.href} rel="noreferrer">{publication.title}</a>
      : publication.title}
  </h3>
  <p class="authors">{publication.authors}</p>
  <p class="venue">{publication.venue}{publication.details && `, ${publication.details}`}</p>
  {publication.abstract && (
    <details>
      <summary>Abstract</summary>
      <p>{publication.abstract}</p>
    </details>
  )}
</article>
